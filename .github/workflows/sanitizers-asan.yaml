name: Asan sanitizer

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allow manual triggering

jobs:
  build:
    uses: ./.github/workflows/build-on-commit.yaml
    with:
      preset: "SanitizeAsan"
